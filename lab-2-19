# In two lines, take in a list of 5 floating point numbers and return the: Max, Min, Sum, and Average
num_list = [float(input()) for _ in range(5)]
print(f'Max = {max(num_list)}\nMin = {min(num_list)}\nSum = {sum(num_list)}\nAverage={sum(num_list)/len(num_list)}\n')
